# Copiar a `movil/.env` con los valores reales. Ese archivo está en .gitignore.
# Son los MISMOS valores que `.env.local` de la web, con otro prefijo: Expo
# solo expone al bundle las variables que empiezan con EXPO_PUBLIC_.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
